export const environment = {
  production: true,
  apiUrl: '/api', // Relative path for production (served from same domain)
  apiBaseUrl: '', // Empty for production (same domain)
  appName: 'Resume Evaluator',
  version: '1.0.0'
}; 